# netdata-utils

## Usage
```
$ netdata-utils -h
usage: netdata-utils [-h] {dump} urls [urls ...]

A set of netdata utilities

positional arguments:
  {dump}      List of possible actions
  urls        netdata API URL e.g., http://localhost:19999

optional arguments:
  -h, --help  show this help message and exit
```
